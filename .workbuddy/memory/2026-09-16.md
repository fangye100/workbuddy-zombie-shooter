# 2026-09-16

## GPT 独立精化（ad4f63f）
- 在"三轮全绿"的代码上又找出一轮实质缺陷 → `mr-foot-2`：秒制平滑、最终重解口径的修正指标、
  独立测量锚点、内外可达域与收敛守门；新增 `MT/bake-acceptance.test.ts` 与 docs/16B。
- 反思（用户要求）：修探针的**类**不修字面；拿实现观察面当 oracle 是自证；补参考系不变式/
  随机性质测试要**交付前自己先扮演审核方**写好，而不是留给外部循环第 N 轮。

## MR-06 会话与工作台交付（6b58632 + 438783e）
- 新 `services/binding/retarget-session.ts`：两入口（绑定面板 fit / 场景物体骨架）统一会话。
  契约：输入变→stale；消费点（导出/应用/exportDryRun）过 `guardedAnimForConsumption`
  （requireResult + 入口 A syncTarget unchanged 校验）；失败/取消保留 lastGood；
  配方标定缺失拒绝（R13 会话层，后收窄为"同输入缺标定才拒"）。
- 新 `retarget-workbench.ts`（右侧 720px dock）：状态徽章（通过/部分完成/失败/待更新）、
  源·目标双视口投影、接触时间轴、标定 sidecar 载入/保存、动作位移纠正（setSourceRootMotion）。
- main.ts：L0 retargetBvh 只留映射诊断，动画产物一律走新管线；`hook.anim` 形状兼容冒烟
  （45°消偏/22骨/23轨道），新增 `hook.anim.session`。
- contracts/pipeline 扩展：RetargetOutcome.segments + constraintResiduals（逐约束残差）。

## UX 实测两轮（用户真实浏览器验收）
- 一轮 4×P1+4×P2（ed5c94e/efc3299）：首载空白=先画后 resize；错误载入保留工作状态+一次性通知；
  时间轴点击扣 TL_LABEL_W；未评估指标显示"未评估"不给 0.00mm；入口 B 重生成自动重挂载。
- 二轮 2×P1+2×P2（78bf2a8/1f8ef62）：标定归属几何验明 + src/tgt 双通道诊断 + 源侧警告指纹过期。

## 标定判据长链（b98e991→aafa168，每轮都有判别力实验的回归在册）
- 坐标规则（b98e991）：判据只用**骨盆相对骨架几何**——根 OFFSET 不是世界骨盆高（有位置通道时
  采样世界根由通道决定）；目标基线必须带标定自己的单位声明（calibrationUnitsOnly）。
- 单位/契约/单侧腿（b840ac6→13b9bd2）：单位上下文独立保留（targetUnitCtx）；pelvisHeightM
  按契约=骨盆到支撑面相对量（不再二次减 planeY）；判据=链推导**足类**（3 骨腿链末端骨）逐标记
  ±35% 带宽——单侧拉长/缩短都在该标记自身暴露（min-only 被手部劫持、max-only 被未变侧掩盖，
  两个方向都翻过车）。
- 资产归属（84d6007/0bda7ae）：targetCalOwner 前移到几何检查之前——等比跨资产骨架（A/B 原始
  数字相同仅键不同）让几何检查必然通过，只有归属能拦；差分 oracle 用 pelvisHeightM（planeY
  对单位零敏感、恒真）；无目标载入目标标定直接拒绝。
- 事务化（b778631/aafa168）：setTarget/syncTarget/setTargetCalibration 全部"失败完整回滚"
  （snapshotTargetCalState 五字段）；SUSPECT 诊断自限不累积。
- 用户侧复审 05058af 确认通过（专项探针 5/5）——标定与会话修复链端到端闭合，共 16 轮审核。

## 元教训（写进流程）
- 🔴 **判别力实验是交付前置**：每条新回归自证"禁用修复→测试失败→恢复全过"；
  差分 oracle 的观测量必须对被测错误敏感。
- 🔴 **先审后记**：docs 状态行不得预写"复审 PASS"。
- 审核探针（旧实现跑新测试 / 禁用检查跑新回归）是最有效的防自证手段，用户侧 Luna 也在用。

## 终态
- 全仓 728 测试、typecheck 0、editor:build 过、浏览器定向 verify-workbench.mjs 34/34
  （A–L 段：含标定回路/换骨架停用/重生成自动重挂载/导出守门）。
- 冒烟既有 6 项失败（场景物体数期望 5 + 自动半径 1）经 HEAD 复跑证实与本交付无关。
- 剩余主线：真实 mocap 2m/0.5m 蒙皮动作验收（用户明确定义未完成）+ 首次标定创建 UI（视口拾取）。

## WU 队列独立评审：三轮，最终通过

- 三轮只读评审（A 架构铁律 / B 证据可复现 / C 代码风险），报告已入库 `docs/review/`。
  第一轮 A、B 不通过（6 条阻断）；第二轮 A 不通过（整改引入 typecheck 红 + 保存自检误拒）；
  第三轮 A、C **通过**。
- 六条阻断的修：A1 StepReport 真值 + W_SPAWN_CAPACITY 运行期诊断 + UI 消费者；
  A2 PlaySession 资源登记表（registerResource/ledger，20 次启停 pending==0）；
  A3 pickSceneLights() 纯函数化 + 8 条测试；B1 runtime-parity --compare 入库；
  B5 探针转正 editor-parity.mjs（verify:parity-host）；B7 play-controller.test.ts。
- 🔴 整改自己会引入回归，形态往往是「为了让断言更严，把合法路径也卡死」：
  保存自检误写成「恰好一条」，把连改两个刷怪点再保存这个合法操作拒了。
  每条收紧都要配放开侧用例（多节点编辑必须能存盘）。
- 🔴 改任何 .ts 之后 typecheck 与 test 必须**一起跑** —— 只跑 test 放过了一个类型红。
- 终审：typecheck 0 / test 569 条 32 文件 / editor:build 0 / verify:parity-host 连续 3 次 20 PASS / 0 FAIL。
  分支 23 笔提交全部推送。

## 复审清单 #1–#9 全部闭合（保存/身份/输入/验收四组）

- #1 保存竞态：beginSave/confirmSave，快照之后编辑仍是未保存；#2 外部冲突：磁盘基准指纹比对拒覆盖；#3 Play 编辑边界统一（含异步导入丢弃）+ 恢复抽纯函数 author-snapshot.ts 真实验收；#4 colliderWorldAabb（OBB/椭球精确 AABB，含父级链）；#5 W_SPAWN_DELAY_UNSUPPORTED / W_NAV_DISABLED；#6 runId 三代同检（逻辑身份 vs 操作引用分别定义）；#7 setInput 固定 tick 输入消费 + 障碍推出 + 导航目标重烘 + 箭头键编辑器装配（W/E/R 被 gizmo 占用不能用 WASD）；#8 startPlay/resetPlay 收口所有入口；#9 **窗口遮挡 → rAF 节流成 0** 是验收抖动的真根因，--disable-backgrounding-occluded-windows + bringToFront + parity-runs.jsonl 历史。
- 教训：等待超时必须明确失败；先给可复现条件再修；替身测试/CPU 探针/真实浏览器分开说证明范围。
- 数据：594 条 / 33 文件，verify:parity-host 29 PASS / 0 FAIL，31 笔提交全部推送。

## 复审第二轮（2×P1 + 3×P2）全部闭合

- P1-1 TOCTOU：baseHash 进服务端写入请求，devfs 逐路径写入队列串行，校验+写入原子完成（浏览器预检只剩提前提示）。实测 409 conflict + currentHash 回传。
- P1-2 Play 模型替换漏口：modelReplaceBlocked() 统一边界，含异步完成路径；快照不存网格/骨架 → Play 中禁替换。
- P2-3 撤销记录误删：编辑身份（lastEditId）替代栈长；重复保存 saveInFlight 串行。
- P2-4 旋转胶囊：轴向线段世界投影 + 椭球半径外扩（h4/r0.5 横转 → 半宽 2，修前 0.5）。
- P2-5 失焦按键：blur/visibilitychange 清空并立即提交零输入（失焦后 rAF 可能停，等下一帧等于不提交）。实测失焦后 1.2s 位移 0.00m。
- 数据：597 条 / 33 文件，verify:parity-host 37 PASS / 0 FAIL，33 笔提交全部推送。

## 复审第三轮（写入失败处理 + 协议边界 + verify:fs 回归）

- 队列 bug：存 task.finally() 派生 Promise 却只 await 原始 task → 写入失败进程以 unhandled rejection 崩；清理永不成立。修法：存的/等的/清的是同一个 tail Promise。🔴 教训：Promise 队列里三者必须同一对象，否则不是静默错误，是进程级崩溃。
- 协议边界：/__fs/write 是唯一受协调写路径；409 只保经过 API 的写者；直接 fsp.writeFile 不受保护；rename 前窗口不可消除 —— 协议写进 devfs 文件头，不宣称完整保护。
- verify:fs 回归：import 必须带显式 .ts 扩展名（vite 宽容、Node strip-types 不宽容）。verify:fs 恢复并补并发两写（1×200 + 1×409）与失败恢复（500 后进程/队列存活）用例。
- 数据：verify:fs 24 条全过，test 597/33，34 笔提交全部推送。

## 复审第四轮（Windows 文件身份 + 临时文件独占）

- 队列键按保留大小写路径字符串分桶 → Windows 上 case.json 与 CASE.JSON 进两条队列，实测 200/200 互覆或 200/500 临时文件碰撞。修法：fileIdentityKey = realpathSync + 按平台语义统一大小写（win/darwin 小写，linux 不转）。🔴 队列键必须是文件身份，不是路径字符串。
- 临时名「文件名+pid+毫秒」同毫秒碰撞 → 加随机+单调计数器+wx 独占创建兜底；rename 失败收走临时文件。
- 协议文案修正：409 只存在于 API 内，直接写文件者没有 HTTP 响应。
- 数据：verify:fs 27 项全过（新增大小写并发三条），test 597/33，35 笔提交全部推送。

## 复审第四轮通过 + 非阻断跨平台改进落地

- 评审对 b3d6844 判通过：大小写并发连 12 轮 200/409、rename 失败 500 无残留、verify:fs 28 项。
- 顺手把非阻断改进做掉：isCaseInsensitiveFs 不按平台硬编码，改为在项目根写探针文件、
  用大写变体能否读到来探测真实文件系统能力（缓存；探测失败保守按敏感处理）。
  覆盖 win32 敏感卷/目录、Linux 不敏感共享盘等边缘环境。verify:fs 28 项仍全过。
- 保护范围保持明确：409 只保经过 API、携带基准版本的保存；Agent 直接写文件不在保证内。
- 保存后端整改收尾，36 笔提交全部推送。

## 资料库文档同步（2026-09-16）

- 两个既有线上节点都在个人空间（personal），直接编辑（submit_doc_edit + insert_after 文末追加），无需审阅。
- docs/14 节点（48WumseQVdiYkWOQL2pz94）：追加「三、运行时责任收敛与保存后端（docs/18 · docs/19）」21 块 —— 架构定调 / 八条业务证明 / 四轮复审整改 / 当前门禁 / 未完成清单。
- docs/09 节点（FNfRd1b8idYncNDIdKBmvQ）：追加「8.2 保存后端与编辑器验收的坑」14 块 —— 6 条通用判据（校验与写入同一受控操作 / 编辑历史用身份不用位置 / 队列键用文件身份 / 保护边界说清不说满 / 验收根因常是环境 / Promise 队列三者同一对象）。
- 流程：runtime_context → connect_open_platform 换票 → list-user-spaces 判 category → get_doc_reviews 回读 → 构造组件 actions → submit_doc_edit --dry-run → 正式提交 → get_doc_reviews 只读验收。
- 教训：追加内容用组件语法（Heading/Paragraph/BulletedList/Divider），insert 禁止 Mark ar；文末追加用 insert_after(id='')；dry-run 先校验再正式提交。

## 美术资产替换三阶段（新方向，docs/20）

- 用户诉求：① runtime 胶囊换真角色低模 ② 仿角色设计表做环境/道具设计表+风格渲染图 ③ 混元3D 生成并替换进场景。
- 现状核查：场景 100% 灰盒（22 节点全是 builtin 图元 + s0–s6 共享材质，零外部资产）；
  8 个角色 rigged+animated GLB 已就绪（900–4200 tris）→ 第①步不用重新生成，只需接线。
  MeshSource 已支持 {type:'asset'} → 第③步换环境是纯数据改动。
- 用户决策：NPC 走 **GPU 蒙皮实例化**（一次 draw 上百只带动画）；环境 **全 4 幕一次规划**；参考图用内置图像生成。
- 方案（docs/20）：**烘焙姿态调色板** —— 加载时用现成 evalJointMatrices 按 24fps 逐帧烘关节矩阵，
  运行期零 CPU 重算；实例 12→16 float（paletteBase/poseIndex/flags），顶点加 joints/weights，
  调色板 binding 4；未加载角色自动退回胶囊；分 M1~M4 四期。
- 配额约束：混元图生3D 5 次/天且不能带 prompt。
