# 2026-09-26

## 提交与交接：绑骨面板 proxy 总开关 + 量化参考线 → 已推送，工作区干净

- 用户令（本次会话最后一条）：「先将当前 changes 提交，现在切换到 hunyuan4 对整个分支做质量检查」。
- 提交：工作区只剩 09-23 session 遗留的两个未跟踪文件（`.zcodeignore`、`.workbuddy/memory/2026-09-23.md`），
  已单独立为 `94e6a61` 推送（**刻意不混进 E-01 绑骨改动**，遵循「只 add 自己产出」纪律）。
- 澄清：**会话内无法切换底层模型** ——「切到 hy4」需用户新开会话；本次按 hy4 标准（证据驱动 + 判别力实验 + file:line）做等价审查。
- 交付物：`docs/review/QC-2026-09-26-分支质量检查.md`。

## QC 结论：VERDICT PASS（8 道门禁全绿），1 个 P2 待拍板 + 3 个 P3

- 门禁：typecheck 0 error · vitest **50 files/973 tests** 全绿 · editor:build OK · content:check OK ·
  verify:prefix OK · scene:check 105 资产 + 12 tests OK · mcp-binding:check **71 PASS/0 FAIL**。
- 🔴 **P2（待用户决）**：R2L 镜像后 **LeftHand / LeftLeg / LeftFoot 落在网格外**（射线奇偶 0/6），
  最省修法 LeftHand z−2.0cm、LeftLeg x+2.0/z−2.0cm、LeftFoot x **+1.5cm**（比 README 原写的 2.0cm 更小）。
  三者位移均**小于该骨圆柱半径**（8/10/9cm）→ 蒙皮几乎不受影响；纯属「骨架对称 vs 贴各自网格」的取舍。
  （对照：右侧同名骨均 6/6 在肉里，根因是 E-01 网格本身左右不对称。）
- 🟡 陷阱确认（非缺陷）：22/22 wrapper 都是 `manual=true` —— 因 `setCylinderOffset()` **零偏移也打 manual**
  （binding-session.ts:485）。后果：`autoFit` 恒返回 `changed: []`；误跑 `unpin` 会把解剖半径改成
  「骨长×0.35」，实测差异最大 4 倍（Hips 0.220→0.054、Head 0.160→0.052）。**wrapper 阶段前别顺手 unpin。**
- P3 文档/覆盖：① README:96 自称「16/16 示例」实际只有 12 条；② README:787「Spine\* 0.21」漏了 Spine1=0.22；
  ③ README:20 锚点 `#判据效力排序从强到弱` 失效（目标 :532 是加粗段落不是标题）。
  ④ **新渲染特性（grid/toon/azimuth/top）在 probe.mjs 里 0 条断言** —— 建议固化 11 条判别力断言。
- 已验证一致：README 示例 **9/9 实跑成功**、16 工具与 TOOLS_TABLE 逐项一致、基线表 **56/57** 一致、
  render 新参数判别力 **11/11 PASS**（含 azimuth 越界钳到 ±60、非法 view 回落 front、gridStep 加密）。

## 可复跑的 QC 脚本（在 `.workbuddy/tmp/hy4/qc/`，已 gitignore）

- `insidemesh.py` / `outliers.py`：射线奇偶 + 「最近合法位置」扫描（高模 39854 顶点/79744 三角形）
- `check-render-params.mjs`：直接从 `RgbaImage` 数像素做判别力断言（不依赖 PNG 解码）
- `readme-drift.mjs`：抽 README jsonc 示例用真实 `BindingDomain` 逐个 dispatch（注意签名是 `dispatchTool(domain, name, args)`）
- `check-baseline.mjs`：README 基线表 ↔ sidecar 硬数字比对（56/57）
- `check-anchors.mjs`：README 内部锚点可用性（GitHub slug 规则：**每个空格一个连字符**，非折叠）
- `check-e01-state.mjs`：只读复核 wrapper 状态 / compute_skin 统计

## 教训

- **坑**：`dispatchTool` 的签名是 `(domain, name, args)`，不是 `(name, args)`；`BindingDomain` 需注入 `FsPort`。
- **坑**：临时脚本放 `.workbuddy/tmp/hy4/`，相对路径 `../../../../tools/...` 才对（比直觉多一层）。
- **纪律**：这类「实跑 + 不落盘」的实验做完必须 `git status --porcelain` 验空，证明没污染 sidecar。
- **失效引用**：前一轮交接口径里提到的 `getCylindersForViewport`（称 binding-panel.ts:884），
  在 `.workbuddy/memory/*.md`、**仓库历史和 HEAD 里都不存在**（`git log -S --all` 空），
  实际是 `drawView` 内部调私有 `filteredCylinders`（:1829）—— 以后别再按这个名字找。
