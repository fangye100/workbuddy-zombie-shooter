# 2026-09-19

## 第二轮独立审核（PR #3 bot 修复复核）—— PASS

- 子代理独立审核（general-purpose，103 工具调用 / 17.5 分钟）：**VERDICT: PASS**，一轮通过；
  0 BLOCKER / 0 MAJOR / 3 MINOR。纪律：不给任何结论线索，全部自行复现取证。
- A 修复真实性 20 条逐条落点核实，关键项端到端实测：
  - dynamicInstSlots：真实 GPU 编辑器 Play 探针，3 批各独立 buffer/bind group，Stop 后归 0
  - startIndex=1 → act1/floor-1（作者场景）；4 份 sim 快照 id 与 scenes[] 逐一对齐，
    重跑生成器逐字节复现（仅 createdAt/updatedAt 变化）
  - serve_assets 前缀碰撞 → 403 对抗实测；两个 harness 负例均 exit 1；asset-browser clay 可逆
    （CDP 驱动 DOM：withMap 1→0→1、颜色完整还原）
  - env_transfer 函数级实测 64 个不同色值（旧质心实现每三角形只可能 1 色）；
    gen-asset-meta 删 sidecar 后报缺失并 exit 1（已 git restore 复原）
- B 拒绝项独立复算成立：#10（scenes=8 项，判据 `si >= d.scenes.length`）、
  #20（7304B 早于评审提交，PR base 无此文件，确为 PR 内引入）
- C gh 实时重取 44 条（22 顶层 + 22 回复一一对应），reactions 2 条 -1 与「拒绝 2 条」一致
- D 门禁复跑：typecheck / test(597) / content:check / scene:check / verify:prefix / editor:build
  全 exit 0；verify-batch-lods 90/90、verify-lod-hud ALL PASS
- E 夹带：95 文件 = 76 sidecar + 19 代码/文档，全部可映射到 comment
- 3 条 MINOR：①docs/19 资源清理段与整改后代码相反（已在本轮修复）②87c2b25 计数口径
  21/1 vs 20/2（历史提交，不改）③renderer-core 稀疏槽位防御缺口（唯一调用方已过滤
  count<=0，不可达）

## 文档同步 fe8403c（feature/headless-runtime，已推送）

- docs/19：头部改合并状态（PR #3 / merge 4091ec3）+ 合并后补记；§6 资源清理改为
  「随 Stop 释放（releaseDynamicResources）、2 项/轮、registered==disposed==40」；
  限制 1/2 同步为合并态事实（startIndex 已回改 act1/floor-1，sim 快照恢复派生产物身份）。
- docs/18 §5.6 / §10：启动场景与 smoke 两处遗留条目同步；原「待定决策」标为已定。
- 已提 PR #4（docs/19+18 同步 + 项目记忆 → main，2026-09-19）；合并后 main 即为最新文本。

## 合并态 editor:smoke 实测（--headed，自起自停 5100）

- **127 PASS / 6 FAIL / 3 SKIP（exit 1）**，6 条与报告截点同批，**全是脚本侧问题、非产品回归**：
  - 5 条 = smoke 硬编码 sandbox 期望（物体数 13 / 固定物体名 / category / pickable）vs
    实际启动场景 act1/floor-1（19 物体）——根因已从「startIndex 被改成 7」变为
    「脚本期望值未跟随产品启动场景」。
  - 1 条 = autoFitCylinders 断言自身活引用 bug（LeftArm 读到 0.091 vs 预期 0.0575）。
- 要转绿需二选一：改 smoke 期望值跟随启动场景 / 让 smoke 显式指定 sandbox 场景（产品决策，未动）。
- 5612 资产服务（`assets/_tools/serve_assets.mjs 5612`，PID 15240）是既有残留，未启停。

## PR #4 机器评审闭环（docs 同步进 main）

- 提 PR #4（docs/19+18 同步 + 记忆 + sim-level 提示）→ Copilot（MCP 申请）+ codex 各 1 条 inline，
  等待 4 分钟定型无新增。
- 2/2 **认可**：①codex [P2]：上一轮把「启动场景」写成已修复过头 —— `sim-level.mjs --focus`
  仍写回 startIndex（复现 1 → 7），已改为「仓库残留已修、复发路径未关闭」；②copilot：
  记忆里「未提 PR、main 仍是旧文本」过时 → 改为已提 PR #4。
- 修复 `045d9ab`（docs/19 §6 限制 2、docs/18 §5.6/§10、记忆行、sim-level --focus 写回提示行）；
  门禁 node --check / typecheck / content:check / scene:check / npm test(597) / diff --check 全绿。
- 回复 2/2 + 👍 2 + codex 汇总 comment 收尾；**PR #4 已合并**：merge `8971aa6`，本地 main 已同步。
- ⏳ 未决产品决策：`--focus` 是否保留写回 startIndex（保留+提示 / 拆 `--set-start` / 编辑器侧不改产品文件）。
