# 2026-09-05

## 顶部菜单栏 Skeleton ▸ 绑定动作（用户纠正了之前的「局限于 binding 模块」约束）

> 用户原话：「我记得是让你把这个绑定放在顶部菜单栏里面。比如新开一个菜单叫 Skeleton。
> 在这个菜单栏的 menu 下面有这个绑定的菜单或者说动作」（后补正：菜单名是 Skeleton，不是 Skylark）

### 关键认知修正（重要，避免下次再犯）
之前受「改动局限于 binding 模块」约束影响，我把绑定入口收窄成「面板内蒙皮模式 + 右键进入」，
否决了顶部菜单 + 3D 主视图叠加。**那次否决过度了。** 正确的边界是：
- ✅ **允许**：顶部菜单栏（外壳 UI，改 `index.html`/`main.ts`）作为绑定入口 —— 这只是调用 binding 服务的 UI 壳。
- ❌ **仍禁止**：把绑定**实现**散落到 renderer.ts / picking.ts / 其他 Game Editor 模块；
  以及在 WebGPU 3D 主视图叠加圆柱体 mesh（那会动渲染层）。
- 一句话：**入口在外壳可以，实现在 binding 模块不动。**

### 落地（commit `45b6830`，已提交未 push）
- `apps/editor/index.html`：新增 `#topbar` 顶栏 + `#mainrow` 包裹原三栏；`body` 改 `flex-direction: column`；
  `#gizmo-bar`/`#hud` 的 `top` 改为 `calc(var(--topbar-h,40px) + 12px)` 避开顶栏；
  新增 `.tb-*` 样式（**前缀 `tb-` 不是 `ad-`**，否则被广告拦截 `display:none`）。
- `apps/editor/src/main.ts`：`setupTopMenu()` 接 Skeleton 下拉
  （进入绑定 / 导出 T-pose GLB / 载入 BVH 动画… / 退出绑定），
  全部复用既有 `bindAssetAt` / `openBinding` / `applyBinding` / `loadBvhForBinding` / `closeBinding`；
  「进入绑定」优先资产库选中 `.glb`（`lastAssetPath` 镜像 onSelect）兜底场景选中物体
  （`renderer.state.selectedIndex` —— `renderer.selection` 是 private，别直接用）。
  菜单可用态用 `MutationObserver` 监听 `binding-dock` 的 `open` class 同步。
- `apps/editor/src/services/binding/binding-panel.ts`：新增 `getSmoothWeights()`，
  供菜单「导出 T-pose GLB」沿用面板里的平滑权重开关（纯 binding 模块内改动）。

### 验证全绿
typecheck 0 error · `verify:prefix` 通过（无 `ad-`）· `editor:build` 69 modules · binding 单测 34/34。

### git 纪律
只 `git add` 本任务 3 文件（index.html/main.ts/binding-panel.ts），**未碰**并行 session 在途的
memory/E04 png/docs。`upstream is gone` 异常仍按红线只报告未修（等用户指令）。
未 push（并行 session 同分支，避冲突）。
