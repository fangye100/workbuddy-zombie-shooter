# 2026-09-08

## 绑定（Bind Skin）流程 + UI 面板 · 技术美术评审
- 产出 `docs/15-绑定与蒙皮面板技术美术评审.md`（正文 + 修复顺序表 + 可复用的探针验证手法）。
- 只做 review，**未改任何代码**。探针文件 `apps/editor/src/services/binding/__review_probe.test.ts` 跑完已删，
  `git status` 确认 binding/ 目录干净。
- 实证发现 3 个 P0：
  1. `smoothSkinWeights` 按槽位而非骨 id 平均 → 无关骨实测拿到 18.8% 权重（构造三角面顶点，vitest 跑真实模块）。
  2. 两套权重算法 UI 无切换；`cylinders` 载入即建导致永远走圆柱体路径，LBS 路径 + 3 个参数是死代码。
  3. 无权重可视化、无变形预览。
- 顺带核对骨架：tip 已从 4 根变 **5 根（新增 HeadTip）**，骨架 26 → **27 骨**，已修正 MEMORY.md 过期条目。

## 按评审修复（未提交，卡在 git 仓库损坏）
改动 4 个文件（副本备份在 `.workbuddy/tmp/fixbak/`）：
1. `binding-math.ts` **P0-1**：`smoothSkinWeights` 重写为按 **joint id** 稀疏聚合（定长 key/val 数组 + 线性探测，
   取 top-4 再归一化），不再按槽位平均；新增可选第 6 参 `weld?: {positions, vertexFloats, tolerance?}`
   按坐标量化合并邻接（治 split-normal 硬边撕裂）。
   实测：修复前 `v0: 骨0=0.813 骨5=0.188`，修复后 `骨0=0.625 骨1=0.375`（无关骨归零，Σ 恒 1）。
2. `binding-export.ts`：平滑调用补传 `weld`（用 input.vertices + VF）。
3. `binding-panel.ts` **P0-2**：新增 `WeightMode = 'wrapper' | 'distance'` + 头部下拉 + `getWeightMode()`
   / `setWeightMode()` + `syncWeightModeSelect()`；`BindingEditorData.weightMode` 落盘，`hydrate()` 校验后回填。
4. `main.ts`：导出按 `getWeightMode()==='distance'` 决定传不传 cylinders（让 LBS 路径不再是死代码）；
   `saveBinding()` 写盘前 `readProjectFile` + `validateAssetMeta` 前置校验，不合法/读不到就拒绝写并提示跑 scene:gen。
   - 根因：patch 到不存在的 meta.json 会产出只有 bindingEditor 的非法 sidecar，把 scene:check 打红。
5. `git rm` 掉 `assets/characters/models/E-04/E04_20260901_010134.glb.meta.json`
   （40MB 原始产物命中 gen 的 `RAW_SOURCE_RE`，本就不该有 sidecar；备份 `.workbuddy/tmp/E04_raw_meta_backup_20260908.json`）。

门禁：typecheck ✓ · vitest 442/442 ✓ · editor:build ✓ · scene:check ✓ · content:check ✓ · verify:prefix ✓
冒烟：118 PASS / 4 FAIL —— 4 项全在主 3D 视口圆柱体叠加层（`verts=0`）与 `autoFitCylinders`，
**与本次改动无交集**（视口走 `getSkeletonOverlaySource`+`buildCylinderOverlay`），CONSOLE ERRORS 0 / EXCEPTIONS 0。
A/B 验证未做成，见下。

## 🔴 事故：`.git/refs/` 目录消失，git 完全不可用
- 起因：`git stash push -m "wip-binding-fix-ab-test" -- <4 文件>` 被 Bash 工具 120s 默认超时 **SIGTERM 强杀**。
  之后 `git status` 报 `detected dubious ownership in repository at 'C:/'`，`git --git-dir=.git` 报 `not a git repository`。
- 只读诊断：`.git/` 只剩 COMMIT_EDITMSG / FETCH_HEAD / HEAD / ORIG_HEAD / config / description / hooks / index / info /
  lfs / objects —— **`refs/` 与 `packed-refs` 均不存在**；无 `*.lock`；`GIT_DIR`/`GIT_WORK_TREE` 为空。
- 已停止一切 git 操作，**未做任何修复**（遵守「未经许可不碰 .git 原始数据」铁律）。
- 4 个改动文件完好（工作区 == `.workbuddy/tmp/fixbak/` 副本），未丢失。
- **教训：不要用会 SIGTERM 的方式跑 git stash**（长命令务必 `run_in_background` 或显式加大 timeout）。
  改 4 文件后 stash 本不该跑那么久，怀疑同时有并行 session 在操作同一仓库。

### 损坏评估（用户授权查 remote 后，只读诊断）
- 根因推断：`git stash push` 触发 `gc --auto`，gc 重写 pack 途中被 SIGTERM → **旧 .pack 已删、新 .pack 未写完**。
  证据：`.git/objects/pack/` 只剩 `.idx` + `multi-pack-index`（均 9-7 07:39），**`.pack` 数据文件不存在**；
  loose 对象 **0 个**（13/17/1c… 全是空目录）；`ORIG_HEAD`(d3fd3cd5)、`FETCH_HEAD`(2c8550fe) 指向的对象均不存在。
- **工作区文件 100% 未丢**：全量备份 `C:\Users\fangy\WorkBuddy\_zombie_backup\worktree_20260908_1120.tar`（2.4GB，含 assets，排除 node_modules/.git/dist）。
  LFS 是大文件实体不是指针（E04 glb 38MB 完整）。
- **远端完好**：`origin/main = c763d603`（S2a userData 转正 v1→v2）。远端历史含 binding 早期提交（b33ffe2/212dde2/44c3de0/020f06e…）。
- **丢失 = 本地未推送的 5 个提交的 commit 对象**：eba2a17 → 25ba60e → 2443f98 → a99c721 → 5986d15。
  内容都在工作区（工作区即这些之后的状态），但作者/时间/message/增量历史没了，重建后只能合成 1 个新提交。
- **无分叉风险**：工作区含远端最新 S2a（document.ts `SCHEMA_VERSION=2`、default.scene.json `schemaVersion:2`）
  → 本地只是**领先**远端，不是分叉。重建后工作区差异 = 上述 5 提交 + 今日修复。


## 12:00 git 仓库恢复完成 + E-04 资产找回 + 提交纪律入册

### 恢复结果
- 新 .git 从远端 clone（--no-checkout，必须用 C:/ 风格路径），旧 .git 隔离为 .git.broken-20260908，
  用 .git/info/exclude 本地排除。git read-tree HEAD 填 index（--no-checkout 不写 index）。
- 分三笔提交并逐笔 push：
  455ffa3 恢复 9/5-9/7 未推送工作（31 文件，+4415/-290）
  fec5fef 今日绑定/蒙皮 P0 修复（7 文件，+1609/-124）
  fc321bc AGENTS.md 新增「提交即推送」铁律
- 远端从 c763d60（9/4 23:54）推进到 fc321bc。

### 关键发现：E-04 资产曾被连带删除
- 恢复后 status 出现 57 个 D，全是 assets/characters/models/E-04/**。
- 决定性证据：损坏瞬间的 .git/index（11:06）里这 57 个条目**仍在** → 不是本地提交有意删的，
  而是 11:06-11:20（stash 事故窗口）从工作区消失的。
- 11:20 的全量备份 tar 里也只有 images/E-04 没有 models/E-04，印证同一时间窗。
- git checkout HEAD -- assets/characters/models/E-04/ 全量恢复，LFS smudge 成功
  （fbx 39.7MB / glb 39.9MB / zip 35.2MB 均为真实内容，非指针）。
- 连带收益：冒烟从 118 PASS/4 FAIL 改善到 123 PASS/1 FAIL —— 之前 4 个失败里
  有 3 个正是因为视口加载不到 E-04 模型。

### 门禁（恢复后全跑）
typecheck ✓ · npm test 442/442 ✓ · editor:build ✓ · content:check ✓ · verify:prefix ✓
scene:gen 28 sidecar 无变化 ✓ · scene:check 12/12 ✓
editor:smoke 123 PASS / 1 FAIL / 3 SKIP，CONSOLE ERRORS 0 / EXCEPTIONS 0
- 唯一 FAIL：autoFitCylinders 断言读 LeftArm 拿到旧值 0.091，而钩子 oRadiiImmediate/Persist
  都已是正确 0.057492 —— 断言读取路径与半径表不同步，非本次改动引入，留作独立跟进。

### 规则变更
AGENTS.md §3 新增「提交即推送」：本地每产生一个 commit 必须当场 push，多笔逐笔推；
push 失败必须当场解决或上报。同时记录「长 git 操作必须后台跑」的教训。


## 绑定面板 UI/UX 重分组落地（docs/15 §3.1–3.6，commit 53a5e32 已推送）
- 头部 12 控件 → 编辑/镜像/姿态/产出/保存 5 组（bd-glabel + 竖分隔线，组内 nowrap）；
  重置/Detach 降级 bd-btn.danger 且均二次确认；半径=滑块+数字框双向同步+Shift方向键10×；
  禁用按钮补 title；包裹器开关提头部；10 行帮助折叠进「?」；新增「●未导出/✓已绑定」常驻徽标
  （实现=编辑指纹 editSig() 与 boundSig 比对，挂在 refresh() 单一出口，比逐改动点插 markDirty 不易漏）。
- 🔴 教训1：**range 的 max 不能做「随骨长自适应」**——会静默钳制写入值（0.3→0.155），
  违反「禁止静默修数据」且同一滑块位置含义随骨漂移；已试已撤，精确输入交给 number 框（不钳制）。
- 🔴 教训2：冒烟 L2g「拖圆柱体=改半径」原写法取第一个命中点，垂距仅 2.5px → 被判
  「抓核心=沿轴移动」，且固定向下拖对垂直骨轴是退化方向（垂距不变）。修法=扫全格取
  **垂距最大**的边缘点 + 沿**远离骨轴**的法向拖。新增钩子：binding-panel.segmentScreen / main.ts wrappers.axis。
- 冒烟新增 9 条 DOM 断言锁死重分组。门禁 8 道：smoke 132 PASS/1 FAIL（唯一失败仍是已知的
  autoFitCylinders 断言读取路径不同步，非本次引入）。
- 遗留：docs/15 提交时行尾双空格硬换行会被规范化掉（工作区与 HEAD 差 89 行纯空白，git 自动处理，无害）。
