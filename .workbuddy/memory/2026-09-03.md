# 2026-09-03

## Game Editor 重设计技术选型 SOT → 资料库（已落地）
- 将上一轮「背景渐变 + 地面网格 + UI 右栏拆分」改造中的技术选型/设计整理为 **SOT（Source of Truth）** 文档，存入资料库（个人文档，默认空间，免团队确认）。
- 文档覆盖 6 块：①ADR-001 引擎零改动铁律与判据；②天空穹顶球渐变方案；③编辑器独占 REPEAT sampler + Canvas2D 网格纹理；④Unity 式右栏 Tab 拆分（含 Tab 点击缺口修复）；⑤灯光柔化默认参数表；⑥无头 WebGPU 验证方法论 + 反模式清单。
- 资料库写入走 `library` skill：`runtime_context.py`→client 模式→`connect_open_platform` 换票→`doc/create_doc.py --title --content-file`（先 `--dry-run` 校验，再 `--token-stdin` 正式落库）。
- 链接：https://www.workbuddy.cn/space/d/J0fX0StAPHQU3zAFtUTooQ
- 注：项目团队 资料库节点 `FNfRd1b8idYncNDIdKBmvQ`（docs/09 镜像）未用——用户只说「资料库」未给 nodeId，按 skill 默认落个人文档，未触发团队确认。若需挪到项目节点可后续移动。

## Game Editor 重构质量审计 + 加固（docs/12，未提交，留给用户决定）
- **审计对象**：`0ca20fe`(0b.2) → `39675b8`(0b.8D) 共 19 个提交。结论写进 `docs/12-Game-Editor-重构质量审计与加固.md`。
- **两个 P0**：①docs/11 §13.6 声称的「headless 冒烟 30/30」脚本**从未入库**、截图不存在 → 结论不可复现；②引擎层**零测试**，7 个测试文件（97 例）全住在 `apps/editor/src/`，而它们测的都是 packages 代码。
- **量化纠偏**：docs/11 §13.6 称 `LabRenderer`「退化为门面」是夸大——94 方法里仅 31 个 ≤2 行委托，29 个仍含 >10 行实质逻辑（1330 行 / 1945 行 = 68%）；GPU 资源创建 编辑器 20 : 引擎 24。
- **已修 12 项**（详见 docs/12 §3）：测试归位 `packages/{core,scene,render}/test` + `apps/editor/test`；`binding.ts` 上提 packages/render；ADR-005 深相对路径 3 处改 `@aether/*`；5 处描述已删兼容桥的假注释；`normalizeMeshHeight` 上提 packages/scene；npm 脚本 lab→editor（保留别名）+ outDir `dist/editor`；README「禁止 headless」与 docs/10 D4 的矛盾订正为「软渲染不验像素、但可验结构」。
- **新增第四道门禁**：`tools/verify/editor-smoke.mjs`（`npm run editor:smoke`，35 断言，真实 Chrome+SwiftShader+CDP，零第三方依赖，会自起 dev server 并自动探测 http/https）。实测 35 PASS / 0 FAIL / CONSOLE(0) / EXCEPTIONS(0)（含 `--glb` 走真实「导入 GLB…」路径跑满 AnimationService）。
- **四道门禁全绿**：typecheck 0 error / vitest 100/100 / vite build → `dist/editor` / 冒烟 35 PASS。
- **判据陷阱（写新断言必读）**：`setCharacter()` 是替换角色槽不是新增物体 → `getObjectList().length` 导入前后不变，要读 `#model-info`（为此给它加了 id，原本是匿名 `.hint` div）；`#fatal` 常驻 DOM 靠 `style.display` 显形 → 判据必须用 `getComputedStyle().display !== 'none'`，直接读 `innerText` 会拿到标题「无法启动」造成假阳性（自己就踩了一次）。
- **遗留 6 项**（L-1..L-6，刻意不做）：framegraph/RenderFeature 运行时孤儿；`highlight.hovered` 编辑器语义词；`LabRenderer` 剩余 1330 行（推荐下一步做 `packLights/packToon/packPost/packMaterial` 共 200 行装箱下沉 packages/render）；`CHARACTER_HEIGHT_M` 未由 roster 生成；无 pnpm-workspace；docs/10/11/12 未上资料库（线上目前只有 01–05 + GameEditor 4 篇）。
- **新增 ADR-008**（验证资产与结论同入库）、**ADR-009**（测试随被测代码分层归位）。
- **git 纪律**：本轮只改工作区、未 add/commit —— 遵守「未经许可不做 git 写操作」的用户级铁律，提交由用户拍板。

## session 空间错配修复（automation-2026-09-03-00-53-53 → game-design-zombie）
- **症状**：上面这轮审计的执行 session（`374740d9`）被自动化建进了一个自动新建的空目录 `automation-2026-09-03-00-53-53`，用户要求移回 `game-design-zombie`。
- **根因**：`workbuddy.db` 的 `automations.cwds` 里，**只有** `795db6fd`（即本任务）指向那个空目录，其余 5 条自动化都正确指向 `game-design-zombie`。已用 `automation_update` 修正 —— 只改 `sessions.cwd` 的话，下次跑还会再建一个空目录。
- **已迁移三层元数据**：①正文 `~/.workbuddy/projects/c-Users-fangy-WorkBuddy-*/374740d9.jsonl`（复制+路径重写，旧文件保留未动，因为会话还在追加写）；②恢复映射 `~/.workbuddy/sessions/{24372,68064}.json`；③`workbuddy.db` 的 `sessions.cwd` + `automation_runs.source_cwd`/`runs_json`。外加 `changes-index`/`changes-detail`/`artifact-index` 三个索引里的文件路径。
- **备份**：`~/.workbuddy/workbuddy.db.bak-20260903-075145-move-session`（SQLite online backup API，非文件复制）。
- **待用户操作**：完全退出客户端（含托盘图标）后重启，侧栏切到 `game-design-zombie`；若 `sessions.cwd` 被退出时的回写覆盖，再跑一次 `game-design-zombie/.workbuddy/tmp/move-session/sync_session.py`（幂等）。确认无误后可删掉 `automation-2026-09-03-00-53-53` 整个目录。

## L-3 收敛：GPU 装箱下沉 packages/render（第二轮，未提交）
- **做了什么**：`packLights/packToon/packPost/packMaterial`（合计 200 行）从 `apps/editor/src/renderer.ts` 私有方法 → `packages/render/src/frame-uniforms.ts` 的 `packFrameUniforms()` + 四个独立 pack。
- **顺带三件事**：①8 个 layout 常量（`SLOT_BYTES/SLOT_FLOATS/MAX_MATERIAL_SLOTS/MAX_OBJECTS/FRAME_FLOATS/LIGHTS_FLOATS/TOON_FLOATS/POST_FLOATS`）从 `renderer-core.ts` 搬进 `frame-uniforms.ts`，**装箱与 layout 常量同处一屏**；②`srgbToLinear/hexToLinear` 归入 `@aether/core`（原是 renderer.ts 私有函数）；③`renderer-core.ts` 改为 import 并**停止导出**这批常量——否则与 `export * from './frame-uniforms'` 撞名（ESM 星号导出歧义）。
- **关键设计**：引擎侧只声明装箱真正读到的字段（`LightPackParams`/`ToonPackParams`/`PostPackParams`），**不引入 `LabParams`**（ADR-007）。调用方直接传 `LabParams`，它是字段超集，结构化类型天然兼容——既守住依赖方向，又不用在引擎层复制 60 字段参数表。
- **坑（两条路径方向相反，最易写反）**：灯光/材质颜色走 **sRGB→linear**，grading 走 **raw sRGB**（display-referred）。测试专门钉死这条差异。
- **新增 17 条单测** `packages/render/test/frame-uniforms.test.ts`（ADR-009 归位）。总量 100 → **117**。含「装箱后不留 NaN/未初始化槽位」「layout 常量与 buffer 尺寸一致」「packMaterial base 偏移不越界」。
- **四道门禁全绿**：typecheck 0 · vitest 117/117 · build 49 modules/183.50 kB · 冒烟 35 PASS/0 FAIL/CONSOLE(0)/EXCEPTIONS(0)（与重构前基线完全一致）。`renderer.ts` 1946 → **1748** 行。
- **⚠️ 第二轮自检发现的问题（重要）**：docs/12 §4 初版那个「94 方法/31 委托/29 个 >10 行/1330 行/68%」来自**从未入库的临时脚本**——审计报告自己违反了它要立的 ADR-008。本轮把度量脚本固化成 `tools/verify/facade-metric.py`（`npm run verify:facade`）后复算，可复现口径是 **90/58/19/773 实质行**（HEAD `39675b8`），与初版对不上。已订正 §4 表格并加警示。**教训：度量工具必须和结论同时入库，否则连"数字是多少"都会漂移。**
- 新口径下的进展：方法 90→86、>10 行实质逻辑 19→**15**、实质行 773→**626**。D1 达成度 ~85% → **~95%**；剩余 5% 是资源生命周期（`uploadMesh` 46 / `buildGridTexture` 48 / bind group 管理），需重做运行时验证。
- **新增遗留 L-7**：`packPost` 里 grading 三色（`#0E0C16` / `#FFF6E2` / 0.98 倍率）硬编码，等 `content/` 生成层与 L-4 一起解。
- **git 纪律**：本轮同样只改工作区，未 add/commit。

## L-2 收敛：引擎高亮契约去掉鼠标语义词（第二轮）
- `CoreHighlight.selected/hovered` → **`primary`/`secondary`**；`CoreFrameUniforms` 的 `selToon/selMat/hoverToon/hoverMat` → `primaryToon/primaryMat/secondaryToon/secondaryMat`；`RendererCore` 四个 buffer 与 `isSel/isHover` 同步改名。
- **编辑器的 `selected`/`hovered` 命名刻意不改** —— 那是编辑器的领域概念（`state.hoveredIndex`），硬改反而难读。ADR-001 约束的是**引擎的公共签名**，不是调用方内部命名。翻译点只有两处：`features/selection-outline.feature.ts` + `RenderFrameInput.uniforms` 的键名映射。
- **新增 7 条单测** `apps/editor/test/selection-outline.test.ts`（用 fake host，因为 `buildSelectionOutline` 是纯函数、`import type` 不产生运行时依赖）。钉死的关键是「悬停与选中同物体时第二层让位」——引擎里是 `!isPrimary` 短路，映射写反**不报编译错误**，只会让选中物体多画一圈描边，肉眼极难发现。另外还断言了「产出的键必须只有 primary/secondary」，防止语义词重新越过边界。总量 117 → **124**。

## L-1 标注 dormant（第二轮，代码未动）
- `packages/framegraph/src/graph.ts`(295) + `packages/render/src/feature.ts`(130) 共 425 行，**零运行时消费者**。核实：`grep -rn "@aether/framegraph"` 的唯一命中是 `feature.ts` 自己的 `import type`；`RenderFeature` 零实现者。两个 dormant 模块互相引用，谁都没落地。
- 只在两个文件头加 DORMANT 标记（写明可复现的核实命令、当前真实路径是 renderer-core 手写 4-pass、启用前置条件：先补单测再写 driver 做等价验证）。**代码一个字没动**。
- 两个坑记在文档里：①`packages/framegraph/src/**/*.ts` **在** `tsconfig.check.json` 的 include 里 → 「typecheck 通过」不等于「能用」；②`apps/editor/src/features/*.feature.ts` 的 "feature" 与 `RenderFeature` **不是同一个东西**，前者是活的帧输入组装纯函数，后者是 dormant 接口，命名撞车。
- **四道门禁全绿**（含 L-2 后）：typecheck 0 · vitest 124/124 · build 49 modules/183.67 kB · 冒烟 35 PASS/0 FAIL/CONSOLE(0)/EXCEPTIONS(0)。
- **git 纪律**：依然只改工作区，未 add/commit。

## 用户更正：多 session 的 git 提交归属规则（写错 → 已改三处）
- **旧（错）**：「多 session 时 git 写操作只由用户指定的唯一 session 执行，其余 session 只改工作区、不跑 git 写命令。」
- **新（对）**：每个 session 只负责提交**自己业务范围内的修改文件**（只 `git add` 本会话改的，禁止 `git add -A` 一把抓整树吞掉别人在途改动），**不禁止各 session 自行提交**；push 时避开同分支并发推。
- **已改位置**：`agents.md` §2、`.workbuddy/memory/MEMORY.md` 第 5 行、用户级 `~/.workbuddy/MEMORY.md` 铁律段。三处已统一为正解。
