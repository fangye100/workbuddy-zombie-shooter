# 2026-09-23

## session 启动：上下文确认（无业务改动）

- 用户告知：本 worktree 分支正处于「用 MCP 工具做皮肤自动绑定」的实验中，其他 session 在途 → 本 session 只做只读摸底 + 记忆维护，**不碰实验现场**。
- 分支 `followup/pr1-copilot-review`（HEAD 08aa9f6 docs(mcp-binding)+feat(render)）；近期 commit 全在 MCP 绑定实验线（WU-2/WU-3、E-01 MCP 绑定、aether-binding 注册硬化、render toon/azimuthDeg）。
- 🔴 工作区有**未提交**的 `assets/characters/models/E-01/textured/E01_Shambler_900_baked.glb.meta.json`（139+/139-，bindingEditor 的 joint 坐标改动）= 他人在途的绑定现场，**禁止 add/commit/回滚**。
- 维护：MEMORY.md 超限被注入截断 → 重写精简（约 9k → 4.5k 字符），合并重复段、新增「MCP 绑定实验」小节（server 锚点、视觉修正纪律、像素标定、双路径不共享内存态）。
