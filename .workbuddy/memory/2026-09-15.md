# 2026-09-15

## MR-01..05 + bake-adapter 交付（df37fdf..f8e39e8，全部已推送）
- MR-01 5b07ecb：`packages/scene/src/retarget-meta.ts` 持久化契约（标定/配方/指纹/迁移）
  + AssetKind 'bvh' + 运行期 `MR/contracts.ts`。
- MR-02 711cc57：`source-motion.ts`（BVH→规范世界采样、A09 四种根模式）、`rig-calibration.ts`
  （direction/world-rest 双姿态基准）、`space-targets.ts`（S 映射与接触锚点）。
- MR-03 9293d59：`contact-segments.ts` 高度+速度滞回状态机（秒制阈值，不随帧率放大）。
- MR-04 c52bec7：`two-bone-solver.ts` 解析两骨 + `pose-solver.ts` 共享根 GN。
- MR-05 f2397ce：`temporal-solve.ts`/`quality-report.ts`/`pipeline.ts`（平滑后重解、指标口径、取消）。
- MR-06(部分) 7a30b33：`bake-adapter.ts` 世界解→输出骨架节点局部轨道（累计缩放、根容器、指纹守门）。
- 测试在 `apps/editor/test/motion-retarget/`（12 文件），含 review-regression 探针回归 +
  端到端一致性（源→管线→烘焙→测试内独立 FK 读回比对）。618 测试全过。

## 关键运行语义（后续会话必须知道）
- 🔴 接触链路**要求 SourceCalibration.markers 显式提供 .heel/.ball 足底标记**——未标定 =
  MRC_CONTACT_UNCALIBRATED、不做世界锁脚（设计如此，不是 bug；不从动画推导足底，
  否则腾空片段会被伪造成支撑）。
- IK/方向基准保留目标 rest 参考旋转；world-rest 基准禁用于 identity-rest 源（BVH）。

## 审核闭环（当天跑了四层）
- GLM 子代理 3 轮（FAIL→PASS）：修 4b58c8c + f472cb4/b26024f（滑动口径随帧率放大、
  目标侧归一测试空转、守门可绕过 3 个 P1）。
- Codex 外部三轮：R01–R14（b33d0bb：根旋转双重应用、世界姿态混局部、glTF 祖先缩放
  沿链累计、烘焙假验证等）→ 5 P1（91311f3+37e6ac7：IK 丢参考旋转、标定身份、
  首帧伪造接触）→ 2 P1+1 P2（f8e39e8：脚世界朝向 W'=Q_src·D、能力缺口计入状态、
  叶子骨 D=D_parent·restLocalR）→ 精确回归 1fd2bdb+749dd66。
- 元教训：API 形状测试 ≠ 动画验收——读回 oracle 不能用被测代码的 helper（自证）；
  审核探针数值应逐个变成回归断言；**typecheck 归零是 commit 前置**（vitest 全绿会掩盖类型错误）。

## 环境三定案（全部落 AGENTS.md）
- c1f81f3：包管理器切 **pnpm 9**（用户拍板方案 A），锁文件入库，禁 npm/yarn。
- 762d0a4：TS5.9 泛型 TypedArray 收紧——修法是**源头标注 `<ArrayBuffer>`**（~19 文件），不是调用点 cast。
- 052d83f：worktree 迁移后必须 `git lfs pull`——scene:check 28 项失配的真因是 LFS 未 smudge
  （172 个文件全是指针文本），**指针态禁跑 scene:gen**（会用 meta 去修假的失配）。

## 并行提示
- 主 checkout 同日在 `feature/headless-runtime` 分支做 WU-0..WU-6（headless runtime/刷怪点/Play），
  各写各的记忆日志互不干扰；本 worktree 只管 retargeting 分支。
