# 2026-09-20

## PR#5：retargeting 重新合入 main（修复 09-17 强推覆盖事故）

**事故考古**：PR#1/#2 曾于 09-17 00:11/00:28 合入 main（merge 3f1529cd/3ebab1cd）；同日 14:30
本机 push 平行旧线 fc2c68e（09-14 重建线，无 retargeting、无两线合流）强推覆盖远端 main，
两个 PR 的成果被从 main 历史抹掉。PR#3/#4（headless-runtime）合在重写后的 main 上。
本机 09-14 18:27 后一直没 fetch，是推错线的根因之一——**长会话开工先 fetch --prune**。

**流程**（worktree-01，分支 followup/pr1-copilot-review）：
- 先把重写后 main（8971aa6）合入分支再提 PR（450d21a 先例）。冲突仅 4 个记忆文件
  （平行线各自写了同名日期日志）：09-14/15/16 取并集；MEMORY.md 编辑器小节逐条取详者。
- 合并后 typecheck 报错：两线在 renderer-core.ts 的自动合并把 TS5.9 `<ArrayBuffer>` 收紧
  （retargeting 线 762d0a4）与 headless 侧裸类型（CoreDynamicBatch/BatchSlot）汇到一起 →
  源头补标注（2e20317），零运行时改动。
- 883 测试全绿；editor:smoke 125P/8F 与**合并前 35f104f 逐字一致**（含数值）→ 8 个 FAIL
  全是合并前既有脚本侧问题（5 sandbox 陈旧期望 + autoFit 活引用 + 时序 flake + 像素阈值边缘），
  非产品回归。**冒烟对照法（同脚本跑合并前基线）是判定合并回归的最快手段**。

**bot 评审闭环**（connector 2P1+1P2 + Copilot 1，4 条全回复）：
- 认可：animReport 提交早于 setTarget（目标失败不回滚 UI 报告）→ 移到成功后（两入口）；
  mid-chain 非关节中间节点折叠进 bake rest 但播放 FK 走节点层级会二次应用 →
  MRS_NONJOINT_INTERMEDIATE 显式拒绝（身份不拦、根容器 rootParentWorld 不拦）；
  sourceRestDirections 缺映射感知选子 → 补 firstHumanikChild 优选（与 bvhRestDirections 同规则）。
- 部分认可：normalization 忽略问题——认其隐患（无标定 Z-up 静默按 Y-up）→ 加
  MRR_NORMALIZATION_IGNORED warning；拒其整体推导提案（normMat 含居中平移+展示重缩放，
  不是资产身份；R 符号绑定混元 +Z 朝下约定，盲推 180° 错基）。
- 回归 +8，883 全过，typecheck/build/content/scene/prefix 绿。
- merge commit f9e1be8。origin/feature/headless-runtime 只剩遗留记忆提交 43ea501（主 checkout 管辖）。

**坑**：`git diff --check` 对 CRLF 文件新增行必报 trailing whitespace（仓库 *.ts 全 CRLF、
无 text 属性、autocrlf=false）——固有噪音，不在 8 道门禁内，勿据此改行尾。
