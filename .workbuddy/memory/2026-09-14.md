# 2026-09-14

## 重定向开发计划定稿（docs/16 修订 + 16A 归档）
- 分支 `codex/retargeting-research-plan-20260914` 在本 worktree 建立，专职 retargeting。
- 956ea35/46727c1：更新 docs/16 开发队列（MR-01..08 单元表 + A01–A18 验收矩阵 + 失败矩阵），
  算法研究归档为 docs/16A-Retargeting运动空间补偿算法研究.md（HumanIK 公开证据、可复现公式、
  二维数值验证脚本）。
- 02aaa76：按独立审核意见补齐两块契约——标定持久化（RetargetCalibration/Recipe 指纹与失效规则）
  与烘焙转换契约（世界解→输出骨架局部 TRS、非均匀缩放显式拒绝）。
- （勘误 09-17：57e5a90 灯光修复实际提交在 main 上，非本分支——当时 `git log --all` 混入了其它 ref，本分支未含该提交。）

## 当日结论
- docs/16 是 retargeting 的**开发合同真源**：owner 表（MR/ 与 MT/ 路径）、验收矩阵、失败矩阵
  后续全部按它执行；`retarget-session.ts` 放 `binding/` 而非 `motion-retarget/` 内是**有意的**
  （session 编排范围超出算法层），实施时禁止当笔误纠正。

## 编辑器灯光修复（场景环境接线）— 57e5a90 已推送

**根因**：编辑器渲染从不消费场景文件里的 `environment`（ambient/hemisphere/fog/rim/exposure）
和 `Light` 组件 —— gen-level 的主题环境（火场暖光/尸潮/暗巷）全部失效，画面永远是
`defaultParams()` 的 key 2.1 + exposure 1.5（过曝）+ 冷灰雾。这就是用户截图「渲染不正常」的主因。

**改动**：
- `renderer.ts`：`SceneLoadResult` 新增 `environment` + `keyLight`（第一个启用的 Light 组件，只有 color/intensity，方向场景 schema 没有）
- `main.ts`：`applySceneEnvironment()` 映射到面板参数 + `panel.syncAll()`；场景是真源，滑块变读写器
- `gen-level.mjs`：场景主光 1.2→1.4（toon lit band ≈ albedo×key×exposure，1.4 接近满色不炸白）；主题半球/环境光各提一档
- `params.ts` s1 `#1B1F2B`→`#707A8C`：原值在主题曝光下被 AgX+grading 暗部压缩推到近黑（实测盒顶 #4F413B），掩体/走廊剪影不可读；#707A8C 实测渲染 #6E6E6E

**试错记录（教训）**：
- 给 background 物件加「雾豁免」（mat.flags.w）后试了天穹背景 → 白 albedo 穹顶 toon 分阶背光半边变灰紫，洗掉主题氛围；且 void plane 同为 background:true 被连带豁免雾，背景变灰 —— **整个撤销**，scene.wgsl.ts 净零 diff。关卡背景就用「虚空底 + 主题雾距离渐变」。
- 若未来真要穹顶雾豁免：background 标志区分不了穹顶和 void，需要新字段或材质级标记。

**验证方法**（可复用）：CDP 探针 `.workbuddy/tmp/light-probe.mjs`（读 `__editor.params` +
`renderer.getSceneSource()` + 截图）、`closeup-probe.mjs`（设相机近景）、`png-sample.mjs`
（最小 PNG 解码采样像素——眼睛被亮绿地板骗了，像素值才说话）。

**门禁**：typecheck / 458 测试 / editor:build / scene:check 全绿。
冒烟 127 PASS / 6 FAIL：全部是陈旧断言（editor-smoke.mjs ~L356-400 硬编码 sandbox
「13 物体/15 节点」期望，但启动场景在 HEAD 上就已是 floor-1=18 物件）+ 已知 autoFitCylinders 遗留。
**待办**：把 editor-smoke 的场景结构断言改成从实际加载的场景文件派生。

**环境坑（本次）**：bash shim 的 PATH 坏（ls/dirname 找不到）→ 命令前手动
`export PATH="/c/Users/fangy/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:/c/Windows/System32:$PATH"`；
PowerShell 工具有输出无回显（side effect 正常）。CDP：本机 sandbox 代理拦 curl 127.0.0.1，
node http 直连才行；新起 Chrome（9398/9397）静默不监听调试端口（Chrome 152 限制收紧？），
复用用户日常浏览器 9336 实例 + `/json/new` 开自己的 tab 才通（证书拦截走 MagicDNS 域名避开）。
