# 2026-09-02

## Game Editor：资产库 Asset Library + 属性 Inspector（commit e8e616d）

- **布局**：index.html 改三栏 + 底部 dock —— 左面板 | 中央（画布+资产 dock）| 右 Inspector。
  四条分界线全部可拖拽（左栏右缘 / Inspector 左缘 / dock 顶缘 / 树-内容分隔条），
  尺寸走 `:root` CSS 变量（--panel-w/--insp-w/--dock-h/--tree-w），localStorage 持久化。
- **文件系统 API**：vite.config.ts 加 `/__fs/list`（单层目录 JSON，懒加载）+ `/__fs/file`
  （原始流）。根=项目根，`resolveInside()` 小写比较防越界（curl 实测 ../../ 与绝对路径都 404）。
  **只读**，不打算加写操作。
- **asset-browser.ts**：目录树懒加载 + selectDir 时 expandTo 自动展开树链（否则程序化跳目录
  树上看不到自己在哪）；内容区滑杆 0=文件名列表（表头+大小+时间列）、1-100=图标网格
  （--ad-cell 74→152px 连续）；图片直接 `<img src="/__fs/file?path=...">` 真缩略图。
- **asset-inspector.ts**：异步竞态用自增 token 防「快速连选两个资产时旧结果盖新结果」。
  GLB 选中即 parseGlb 出顶点/面/子网格/身高；`.obj/.fbx/.gltf` 提示只支持 .glb。
- **renderer.addObject**：拖入 GLB 生成**新物体**（与 setCharacter 替换角色槽位是两条路）。
  墓碑槽位优先复用（下标不变、变换槽位布局不受打扰），满了才 push（MAX_OBJECTS=64 硬上限）。
  SceneObject 加 `ownsTexture`：独占贴图随 removeObject/destroy 销毁，whiteTex/charTexture 不动。
  画布拖放落点 = pointerRay 与地面 y=0 交点；重名自动 ` 2` ` 3` 编号。
- **splitter.ts 两个坑**：① 抓取偏移补偿 —— 不按「起始值+指针位移」算的话，从把手中央
  抓起会把尺寸凭空顶出半个把手宽；② 依赖 window 尺寸的 max 必须传**函数**每次现算，
  静态值在窗口缩放/无头小窗下会过期夹错（headless 600px 窗口实测踩中）。
- **验证**：`Temp/game-editor-check/cdp-assets.mjs` 30/30 通过（合成 PointerEvent 测拖拽、
  `new DataTransfer()` + `new DragEvent('drop')` 测画布拖放 —— 不需要真实 DnD）。
  vitest 69/69，typecheck/build 全绿。截图存档 `apps/lab/shader-lab/screenshot-editor-asset-library.png`。
- 注意：本会话期间 .git 对象库损坏过一次，已由恢复流程重建基线（ac40f67，含当时全部 WIP），
  损坏原件在 `.git.corrupt-20260902/` 备查。

## 材质绑定继承 + GLB 层级树（material-property-panel 分支，本会话）
- **灾难恢复后续**：旧 worktree 挪 `_wt-old-panel` 抢救出 4 个无冲突文件（binding.ts/binding.test.ts/
  gltf.ts/main.ts），renderer.ts 因基线混入另一会话 addObject 工作只能手工重放（旧 blob 已随对象库丢失）。
  全部重放完毕、逐文件 diff 确认后 `_wt-old-panel` 已删；救援 tar 留在 `WorkBuddy/_git-rescue/`。
- **worktree 依赖坑**：npm 装的 node_modules 解析出 typescript@5.9.3 + @webgpu/types@0.1.72，
  触发 `Float32Array<ArrayBufferLike>` vs `GPUAllowSharedBufferSource` 一片 TS2345（TS5.7+ 泛型
  TypedArray 与新版 webgpu types 不兼容）。主仓 pnpm 钉在 5.6.3/0.1.49 零错误。
  **修法**：`npm install --no-save typescript@5.6.3 @webgpu/types@0.1.49`（仓库不跟踪任何 lockfile）。
- **binding.ts 局部类型漏改**：MatchResult.inherited 改过 `((P|null)[]|null)[]` 但 matchBindings 里
  局部 `inherited` 声明没跟上 → TS2322。一处改齐。
- **E2E 验证脚本** `Temp/game-editor-check/cdp-hier-binding.mjs`：内存现造 GLB-A/B（层级+改名+
  auto-ID 漂移），经 `input.files = new DataTransfer().files` 注入导入流，22/22 断言。
  关键断言：树还原（组行缩进 30/44/58px、空叶剪掉）、重导同档 3 节点按 ID 继承、
  改名档 1 按 ID + 2 按路径、override 跨替换保留、组行折叠。
- 提交 `885cb3c`（material-property-panel，基于恢复基线 ac40f67），968 insertions。
