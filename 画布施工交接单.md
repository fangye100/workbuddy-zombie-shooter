# 画布施工交接单

> 给**新会话**用。开场直接说：
> **「读 `画布施工交接单.md` 和 `assets/characters/roster.json`，按交接单施工。」**
> 不用重讲背景，所有节点 ID / 坐标 / 结构都在本文件里。

设计文件：https://ardot.tencent.com/file/720788949675822?node_id=0%3A1
文件名：**末日尸潮 · 横屏肉鸽射击 UI**

---

## 0. 前提

需要 ardot 画布工具。**当前会话搜不到就跳过本文档**，改做 `美术素材生成任务.md`。
两条线不互相阻塞，别在环境上耗调用。

开工前只验证一次：

```
ToolSearch({ tool_names: ["mcp__ardot__open_design"] })
```

搜到就开工：

```
mcp__ardot__open_design({fileId: "720788949675822"})
mcp__ardot__fetch_editor_state({includeSchema: false})
```

`batch_edit` / `capture_screenshot` / `fetch_editor_state` 等 20+ 个工具
**要等 `open_design` 打开编辑器面板后才动态注册**，之前搜不到是正常的，不是缺失。

### 0.1 施工载荷（优先走这条）

`assets/characters/ardot_batch_edit.md` —— 9 个 Frame 的 `batch_edit` 逐条脚本，
**21 个代码块 / 380 ops**，每个角色 Frame 拆成 2 步、索引页拆成 5 步，全部 ≤25 ops。
坐标、配色、AI 提示词都从真源生成好了，**只需把上一步返回的节点 ID 填进 `<<...>>` 占位符**。

`assets/characters/角色Frame施工图.html` 退居人工评审底稿（9 个 Frame 的 1:1 复刻）。

### 0.2 写 batch_edit 前必读

权威文档：

```
~/.workbuddy/plugins/cache/workbuddy-builtin/skill-ardot-design-core/0.1.2/
├── references/ardot-schema.md   节点属性 schema
├── rules/design-rules.md        fills / effects / 坐标 / 文本
└── tool-usage/batch-edit.md     ★ I / C / U / M / D / G 语法 + 禁用属性表
```

最容易踩的几条：

- **binding 名只在单次 `batch_edit` 调用内有效**。跨批次必须用上一步返回的真实节点 ID。
- 每次调用 **最多 25 个操作**；一批里任一操作失败 → **整批回滚**。
- 禁用属性：`textColor` / `fillColor` / `backgroundColor` / `alignItems` /
  `justifyContent` / `borderRadius` / `verticalAlign` / `color`。
  正确写法：文字颜色用 `fill`，圆角用 `cornerRadius`，对齐用
  `primaryAxisAlignItems` / `counterAxisAlignItems`，字重用**数字字符串**（`"700"`）。
- **没有 image 节点类型**。图是给 frame / rectangle 打 fill：
  `G(nodeId, "ai" | "placeholder", prompt)`。
- 文字节点默认**没有颜色**，必须显式给 `fill`。
- 硬边投影：`{type:"DROP_SHADOW", color:{...a:1}, offset:{x:6,y:6}, radius:0, spread:0}`
  （`radius: 0` 就是硬边）。

---

## 0.5 数据勘误（2026-08-31 校验）

| 项 | 问题 | 状态 |
|---|---|---|
| E-03 英文名 | 本表原写 `Retcher`，`roster.json` 是 `Spitter`。以 roster 为准 | 已修正 |
| E-04 accent | `rare-common` = `#9AA0A6`，与 `text-dim` **完全同色**。盾卫威胁度「高」，Badge 用灰会把高威胁画成最低稀有度 | **已修正** → `teal` #2BC4D6（呼应外形里的 teal 焊珠，且与 B-01 血红拉开层级）。`tokens.json` 的 `rare-common` 本身不动，它仍是稀有度色 |
| B-02 / B-03 accent | 同为 `toxic`，且 Page 17/18 相邻 | 可接受（分属 Act2 / Act4），但并排看会缺区分度 |

---

## 任务 A · 角色设计图 9 个 Frame

> **2026-09-01 降级**：角色三视图**不再依赖画布**。
> 交付形态改为「`images/` 里的图 + `角色Frame施工图.html` 审阅页」，见 `美术素材生成任务.md`。
> 画布的角色 Frame 降为**可选**——等 UI 屏（01–09）那边的画布活了顺手做，不为它单独等。
> 本节坐标保留，随时可施工，但**不要因为拿不到画布工具而卡住任何一个任务**。

### 落位（Frame 尺寸 1560×900）

| Page | ID | 角色 | x | y |
|---|---|---|---|---|
| 10 | E-01 | 游荡者 Shambler | 0 | 2640 |
| 11 | E-02 | 扑跃者 Lunger | 1700 | 2640 |
| 12 | E-03 | 呕吐者 Spitter | 3400 | 2640 |
| 13 | E-04 | 盾卫 Bulwark | 0 | 3680 |
| 14 | E-05 | 爆尸 Bloater | 1700 | 3680 |
| 15 | IDX | 敌群图鉴 · 投放矩阵 | 3400 | 3680 |
| 16 | B-01 | 屠夫 THE BUTCHER | 0 | 4720 |
| 17 | B-02 | 母体 THE BROODMOTHER | 1700 | 4720 |
| 18 | B-03 | 零号 PATIENT ZERO | 3400 | 4720 |

已有 01–09 占据 y=60 / 920 / 1780 三行，角色区从 **y=2640** 开始，不冲突。

### 每个角色 Frame 的内部结构（坐标相对 Frame 左上角）

- **标题栏** `h=96`，fill=panel，stroke=ink，strokeWeight=6
  - Badge `x28 y20 w84 h56`，fill=该角色 accent
  - 中文名 `x132 y12 size40`；英文名 `x132 y60 size20`，fill=text-dim
  - 威胁度药丸 `x1300 y26 w232 h44`
- **三张视图卡** `w480 h540`，y=112
  - 正面 · FRONT `x=24` ／ 侧面 · SIDE `x=540` ／ 攻击 · ATTACK `x=1056`
  - 卡内图 `x20 y20 w440 h420`（用 `G()` 生成 AI 图）
  - 卡内标签 `x20 y452 w440 h68`
- **底部面板** `x24 y676 w1512 h200`
  - 4 个数据格 `w372 h64 gap8 y16` → HP / 移速 / 三角面 / 弱点
  - 注释行 `y92 h92`（Boss 用 `bossNoteRows`：`y92 h32 gap6 count3`，三行写三阶段机制）

### 卡内图：优先用已生成的图

`assets/characters/images/<ID>/<view>/<ID>_<view>.png` 已有 16/24 张，
**能复用就复用**，缺的才用 `G()` 打 AI 图。

提示词 = `roster.json` 里该角色的 **`ai.base` + `" " + ai.front|side|attack` + `" " + styleSuffix`**。
`styleSuffix` 在 `roster.json` 顶层，全局共用，不用手写。

卡片标签文字用中文，术语保留英文（例：`正面 · FRONT`）。

### Page 15 图鉴索引

不生成 AI 图。画一张 **4 幕 × 3 波** 的投放矩阵表 + 8 个角色的 48px 缩略剪影对照。
数据来源：`roster.json` 的 `levelStructure` 与每个角色的 `acts` 字段。
**剪影用手工 SVG**（按 `silhouette` 落形，不耗 AI 额度），三视图出完后需回校。

### 施工后必查

1. **E-03 呕吐者 与 E-05 爆尸 是唯一高风险的一对** —— 剪影都是球体，只靠主色（绿 / 黄）二次区分。
   出图后拼在一起看 48px，如果分不出来，就拉开体型比例（E-03 球腹+细腿，E-05 正圆+小头）重出。
2. 每个角色的剪影关键词互不相同，缩到 48px 必须 0.3s 可辨：
   倒三角(E-01) / 水平压缩(E-02) / 球腹细腿(E-03) / 单侧巨块(E-04) / 正圆球(E-05) /
   宽肩长钩(B-01) / 上下双体(B-02) / 瘦高(B-03)。
3. 预警形状语言不能串：扇形(E-01) → 直线(E-02/E-04) → 落点圈(E-03) → 弧形(B-01) → 十字(B-02) → 体态突变(B-03)。

---

## 任务 B · 06–09 god view 参考图截图校验

节点已全部搭好，**只差验证**。逐屏 `capture_screenshot`，下载 PNG 后 Read 出来看。
**截图是内部校验产物，不要落盘到项目目录，不要当作交付物。**

| 屏 | 节点 | 检查重点 |
|---|---|---|
| 06 精英遭遇战 | `2:365` | AI 背景 `2:366` 是否生成；复制来的头像是否错位；标注 an6 可读性 |
| 07 尸潮围攻·濒死 | `2:434` | **红滤镜 `rd7` 必须在索引 2** —— HUD 之下、背景 `2:437` 之上。若 HUD 被压暗就往下移一层 |
| 08 终极技能·尸爆风暴 | `2:485` | 冲击波环 rA/rB/rC 层序；飘字 d1/d2/d3 是否重叠；冷却键 nu8 是否明显区别于可用态 |
| 09 波次清场·战利品 | `2:526` | 三张战利品卡顶部稀有度色条（teal 稀有 / toxic 史诗 / gold 传说）；倒计时 cd 是否压到横幅 bn |

四屏标注文字（若需要重写）：
- an6 `俯角 55° · 角色 1.8m · 精英 3.2m · 4k tris`
- an7 `同屏 180+ · GPU Instancing · 描边顶点色控宽`
- an8 `全屏冲击 · 命中 47 · 顿帧 0.15s · 粒子 2k`
- an9 `掉落光柱 2m · 稀有度决定光色 · 存在 30s`

---

## 已存在的节点 ID 总表

| 屏 | Frame | AI 背景 | 其他 |
|---|---|---|---|
| 01 主菜单 | `2:22` | `2:23` | — |
| 02 战斗 HUD | `2:74` | `2:75` | 头像 AI `2:78` |
| 03 肉鸽三选一 | `2:218` | `2:219` | 图标 `2:233` `2:243` `c3i` |
| 04 补给站 | `2:261` | `2:262` | — |
| 05 结算 | `2:322` | `2:323` | 遮罩 `2:324`（渐变 alpha 0.65） |
| 06 精英遭遇 | `2:365` | `2:366` | 标注 an6 |
| 07 尸潮围攻 | `2:434` | `2:437` | 红滤镜 rd7、警告图标 wi7、标注 an7 |
| 08 尸爆风暴 | `2:485` | `2:486` | rA/rB/rC、d1/d2/d3、nu8、an8 |
| 09 波次清场 | `2:526` | `2:527` | bn、cd、lr（三卡）、图标 `2:539` `2:543` `2:547`、an9 |

页面根：`0:1`

---

## 风格速查（改一个字都要回 `tokens.json`）

**中文** Sarasa Gothic SC ／ **数字英文展示** Inter Black（900）

| 用途 | Token | 值 |
|---|---|---|
| 墨线 / 描边 | ink | `#14110F` |
| 纸张 | paper | `#F5E7C8` |
| 骨白 | bone | `#FFF6E2` |
| 面板底 | night | `#171327` |
| 深底 | night-deep | `#0E0C16` |
| 尸绿 | zombie | `#8FD14F` |
| 血红 | blood | `#E8402A` |
| 警示黄 | warn | `#FFC531` |
| 电光青 | teal | `#2BC4D6` |
| 毒紫 | toxic | `#9B5DE5` |
| 传说金 | gold | `#FF9F1C` |

描边 stroke-ink=4 / stroke-bold=6；圆角 radius-card=18 / radius-panel=24；硬边投影 shadow-hard=6。
按钮统一 4–6px 黑描边 + 6px 硬边投影。面板底 `#221E33`，深底 `#0A0812`，灰字 `#9AA0A6`，冷却灰 `#3A3550` / `#6B6880`。

---

## 关键文件

| 文件 | 作用 |
|---|---|
| `美术素材生成任务.md` | **美术素材生成**（角色三视图 24 张等）。不依赖画布，可独立推进 |
| `assets/characters/roster.json` | **角色唯一真源** —— 外形/数据/攻击/弱点/AI 提示词/画布施工单 |
| `assets/characters/ardot_batch_edit.md` | **画布施工载荷**（已生成）—— 9 个 Frame 的 batch_edit 逐条脚本，21 块 / 380 ops |
| `assets/characters/_tools/gen_ardot_payload.py` | 上表的生成器。改完 roster/tokens 重跑一次，**不要手改 md** |
| `assets/characters/_tools/check_assets.py` | 素材进度核对 + 缺失提示词导出（`--missing` / `--json`） |
| `assets/characters/角色Frame施工图.html` | 9 个 Frame 按本文档坐标 1:1 复刻的评审底稿 |
| `assets/characters/_tools/gen_canvas_preview.py` | 上表的生成器。改完 roster/tokens 重跑一次，**不要手改 HTML** |
| `assets/characters/角色设定圣经.html` | 人类审阅版，含 48px 剪影对照表 |
| `assets/style/tokens.json` | **风格唯一真源** —— 20 token + toonRamp + grading |
| `assets/style/风格圣经.html` | 色板表 / 线宽表 / 明暗阶表 / WGSL 实现 |
| `assets/style/_tools/gen_assets.py` | 改完 tokens.json 跑一次，重生成 .ase 色板 + .cube LUT |
| `.workbuddy/memory/MEMORY.md` | 项目长期记忆 |

**铁律：** 改设定先改 `roster.json`，改风格先改 `tokens.json`。不要直接改 HTML 或画布上的字。
