# 美术素材生成任务

> 给新会话用。开场直接说：**「读 `美术素材生成任务.md`，接着出素材。」**
> 真源是 `assets/characters/roster.json`，本文件只讲**怎么出、出到哪、怎么验收**。
> 提示词不抄在本文档里（抄了就会烂），一律用脚本从真源现拼。

**本文档不依赖 ardot 画布。** 角色三视图的交付形态就是「图片 + HTML 审阅页」，
画布只是可选项，不是前置条件 —— 图都出完了再回去画 Frame 是绕远路。
画布真正的用处是 UI 屏（01–09 的 HUD / 菜单 / 结算），见 `画布施工交接单.md` 任务 B。

---

## 1. 目标

产出 8 个角色的三视图概念图，作为建模、UI 图鉴、投放矩阵评审的统一底料。

- 5 个普通敌：E-01 游荡者 / E-02 扑跃者 / E-03 呕吐者 / E-04 盾卫 / E-05 爆尸
- 3 个 Boss：B-01 屠夫 / B-02 母体 / B-03 零号
- 每角色 3 视图：**正面 front / 侧面 side / 攻击 attack**
- 合计 **24 张**

---

## 2. 产出规范

### 目录与命名（已归一，不要再发明新格式）

```
assets/characters/images/<ID>/<view>/<ID>_<view>.png
```

例：`assets/characters/images/E-03/attack/E-03_attack.png`

- `<ID>`：`E-01`…`E-05`、`B-01`…`B-03`
- `<view>`：`front` / `side` / `attack`
- 文件里**不带时间戳、不带 prompt 摘要**，一个格子永远只有一个文件

### 尺寸

| 视图 | 尺寸 | 说明 |
|---|---|---|
| front | `1024x1536` 竖版 | 全身正投影 |
| side | `1024x1536` 竖版 | 全身侧投影 |
| attack | `1024x1024` 方版 | 攻击视图用方版，卡内图区域 `440x420` 接近方形，方版落地更饱满；三视图统一成竖版需重出全部 8 张攻击图，暂不改 |

### 风格后缀（所有提示词共用，打在末尾）

```
American comic book style zombie game character, thick black ink outlines, bold saturated flat colors, 2-step cel shading, halftone dots in shadows, white clean background, full body, centered, character design turnaround sheet, no text, no UI, no watermark
```

存于 `roster.json` 顶层 `styleSuffix`，**改风格改这一处**。

### 提示词构造规则

```
prompt = ai.base + " " + ai.<front|side|attack> + " " + styleSuffix
```

两段角色描述 + 全局风格后缀。**不要手拼**，跑脚本拿现成的（见 §4）。

---

## 3. 当前进度：16 / 24

| ID | 角色 | 正面 | 侧面 | 攻击 |
|---|---|:--:|:--:|:--:|
| E-01 | 游荡者 | ✅ | ✅ | ✅ |
| E-02 | 扑跃者 | ✅ | ✅ | ✅ |
| E-03 | 呕吐者 | ✅ | ✅ | ✅ |
| E-04 | 盾卫 | ✅ | ✅ | ✅ |
| E-05 | 爆尸 | ✅ | ✅ | ✅ |
| B-01 | 屠夫 | ✅ | ✅ | ✅ |
| B-02 | 母体 | ✅ | ✅ | ✅ |
| B-03 | 零号 | ✅ | ✅ | ✅ |

**24 格全齐。** 进度随时可复跑核对：

```bash
python assets/characters/_tools/check_assets.py
```

---

## 4. 执行步骤（补完 8 张）

### 4.1 拿当前缺失项与提示词

```bash
python assets/characters/_tools/check_assets.py --missing
```

输出每段带 `### <ID> <名> · <视图>` 和 `# 落地: <相对路径>`，提示词整段可直接复制。
只处理 `--missing` 列出的项；已 ✅ 的不要重出，避免风格漂移。

### 4.2 逐张出图

对每条缺失项调用图像生成，参数固定为：

| 参数 | 值 |
|---|---|
| `prompt` | 上一步拿到的整段文本，**原样传入** |
| `size` | `1024x1536` |
| `quality` | `high`（Boss 是主视觉，别省） |
| `background` | `opaque` |
| `output_dir` | `assets/characters/images/<ID>/<view>`（绝对路径） |

出图工具返回的文件名是模型自取的，**必须重命名成 `<ID>_<view>.png`**（同目录下只留这一个文件）。

**不要并行调多张**：`output_dir` 在并行调用时会互相覆盖，还会导致同名文件碰撞。
本次实操已验证：先并行 4 张，结果全落进同一目录、B-02 正/侧撞名只剩一张。
后续必须**一张一张顺序出**。

### 4.3 出完立刻复跑

```bash
python assets/characters/_tools/check_assets.py
```

看到 `24 格全齐` 才算完。顺带会扫出「目录里存在但命名不合规」的 stray 文件。

### 4.4 额度

单张 5–10 额度，8 张约 **40–80**。**开跑前先跟用户确认。**

---

## 5. 验收清单

出完不等于能过。逐条对，任一条不过就重出该张。

### 5.1 剪影（最高优先级）

god view 是 55° 俯角，角色在屏幕上只有几十像素。**缩到 48px 必须 0.3s 可辨。**

| ID | 剪影关键词 | 唯一性 |
|---|---|---|
| E-01 | 倒三角躯干 + 双臂垂过膝 | 长臂 |
| E-02 | 水平压缩 + 四肢外张 | 四足贴地 |
| E-03 | 巨大球腹 + 萎缩细肢 | 球腹细腿 |
| E-04 | 厚重金属矩形 + 单侧巨盾 | 左右强不对称 |
| E-05 | 正圆球 + 无脖子 | 正圆 |
| B-01 | 宽肩倒梯形 + 右臂单点长垂（肉钩） | 单点长垂 |
| B-02 | 上下双体（产卵囊 + 反折上身） | 上下分段 |
| B-03 | 瘦高 + 破碎下摆 | 竖向破碎 |

**已知高风险对**：

- **E-03 呕吐者 vs E-05 爆尸** —— 剪影都是球体，只能靠主色（绿 / 黄）二次区分。
  把两张缩到 48px 并排放，分不开就拉开体型比例重出（E-03 球腹+细腿，E-05 正圆+小头）。
- **B-02 vs B-03** —— 主色同为毒紫 `toxic`（分属 Act2/Act4，可接受），
  但并排看会缺区分度，靠剪影（上下双体 vs 瘦高）拉开。

### 5.2 攻击视图必须读出预警形状

攻击图不是"帅"，是**预警可读性**。每张攻击视图要能看出地面预警形状：

| ID | 预警形状 | 攻击图上要看到 |
|---|---|---|
| E-01 | 扇形 90° | 举臂下挥，地面扇形 |
| E-02 | 直线 | 身体水平拉伸扑出 |
| E-03 | 落点圈 | 酸液抛物线 + 落点 |
| E-04 | 直线冲锋 | 盾板前压、低头 |
| E-05 | 自身发光 | 体表由绿转红、裂纹增亮 |
| B-01 | 弧形 | 肉钩横扫拖出黄色地弧 |
| B-02 | 十字 | 单臂高举砸地、地面十字裂纹 |
| B-03 | 体态突变 | 胸腔裂口张开、扇形毒息 |

### 5.3 通用

- 白底干净、无文字、无水印、无 UI（风格后缀已约束，出图后扫一眼）
- 全身完整，不裁切手脚
- 三视图**同角色要像同一个体**：脸、配色、装备位置一致
- 描边是粗黑墨线，不是软阴影

---

## 6. 后续批次（未开跑，等确认）

角色三视图之外的素材缺口，按优先级排：

| 批次 | 内容 | 数量 | 说明 |
|---|---|---|---|
| P1 | 场景氛围图 · 4 幕 | 4 | 城郊公路 / 废弃仓库 / 地铁隧道 / 生化医院，横屏 1536x1024，用于 01 主菜单与关卡选择背景 |
| P2 | UI 图标 | ~20 | 03 肉鸽三选一、04 补给站、09 战利品卡需要；风格要跟 `tokens.json` 对齐 |
| P3 | 技能特效 · 08 尸爆风暴 | 3 | 冲击波环 / 飘字 / 冷却态，用于验证 god view 可读性 |
| P4 | 48px 剪影 SVG | 8 | 图鉴索引页用；**手工按 `silhouette` 落形，不用 AI 出图**（不耗额度，矢量可缩放） |

P4 是唯一不耗额度的，建议跟角色图一起做掉。

> 48px 剪影验收可先打开 `assets/characters/角色美术素材审阅.html` 看第一章节，
> 把 24 张图缩到真实可读尺寸并排展示，能直接判断哪些角色辨识度不够。

---

## 7. 真源与工具

| 文件 | 作用 |
|---|---|
| `assets/characters/roster.json` | **角色唯一真源**：外形 / 数据 / 攻击 / 弱点 / AI 提示词 / levelStructure |
| `assets/characters/_tools/check_assets.py` | 进度核对 + 缺失提示词导出（`--missing` / `--json`） |
| `assets/style/tokens.json` | **风格唯一真源**：色板 / 线宽 / 明暗阶 |
| `assets/characters/角色美术素材审阅.html` | **主审阅页**：24 张图 + 48px 剪影验收 + 角色卡 + 预警矩阵 + 投放矩阵 |
| `assets/characters/角色设定圣经.html` | 人类审阅版，含 48px 剪影对照表 |
| `assets/characters/角色Frame施工图.html` | 9 个角色 Frame 的 1:1 施工图（画布施工底稿，已嵌入真图） |

**铁律**：改设定先改 `roster.json`，改风格先改 `tokens.json`。
本文档里的进度表是**快照**，以 `check_assets.py` 的实时输出为准。
